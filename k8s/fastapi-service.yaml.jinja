apiVersion: v1
kind: Service
metadata:
  name: fastapi
  namespace: {{ project_slug | replace('_', '-') }}
  labels:
    app: fastapi
spec:
  selector:
    app: fastapi
  ports:
    - name: http
      port: 80
      targetPort: 8000
